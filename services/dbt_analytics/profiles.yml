dbt_analytics:
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('DB_HOST', 'localhost') }}"
      port: "{{ env_var('DB_PORT', '5432') | as_number }}"
      user: "{{ env_var('DB_USER', 'postgres') }}"
      password: "{{ env_var('DB_PASSWORD', 'Lhp542004@') }}"
      dbname: "{{ env_var('DB_DATABASE', 'insuranceWarehouse') }}"
      schema: public  # Default schema
      threads: 4
  target: dev
